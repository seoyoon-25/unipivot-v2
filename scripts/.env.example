# 프로그램 날짜 동기화 스크립트 환경 설정
# 이 파일을 .env로 복사한 후 실제 값으로 수정하세요.

# 기존 데이터베이스 (unipivot.org)
OLD_DATABASE_URL=postgresql://username:password@host:5432/old_database

# 새 데이터베이스 (bestcome.org)
NEW_DATABASE_URL=postgresql://username:password@host:5432/new_database

# Dry-run 모드 (true: 테스트만, false: 실제 실행)
DRY_RUN=true
